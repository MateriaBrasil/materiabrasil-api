---
"$schema": http://json-schema.org/draft-04/hyper-schema
title: MateriaBrasil - Address
description: An address is a place associated with a supplier
stability: prototype
type:
- object
definitions:
  id:
    description: unique identifier of the address
    readOnly: true
    type:
    - integer
  supplier_id:
    description: the id of the supplier of the address
    type:
    - integer
  address_type:
    description: the type the address
    type:
    - string
    enum:
    - manufacture
    - postal
  street_address:
    description: the street address of the address
    type:
    - string
  city:
    description: the city of the address
    type:
    - string
  state:
    description: the state of the address
    type:
    - string
  country:
    description: the country of the address
    type:
    - string
  zip_code:
    description: the zip code of the address
    type:
    - string
    - "null"
  phone_number:
    description: the phone number of the address
    type:
    - string
    - "null"
  identity:
    anyOf:
    - "$ref": "/schemata/address#/definitions/id"
links:
- description: Create a new address.
  href: "/addresses"
  method: POST
  rel: create
  schema:
    additioProperties: false
    properties:
      supplier_id:
        "$ref": "/schemata/address#/definitions/supplier_id"
      address_type:
        "$ref": "/schemata/address#/definitions/address_type"
      street_address:
        "$ref": "/schemata/address#/definitions/street_address"
      city:
        "$ref": "/schemata/address#/definitions/city"
      state:
        "$ref": "/schemata/address#/definitions/state"
      country:
        "$ref": "/schemata/address#/definitions/country"
      zip_code:
        "$ref": "/schemata/address#/definitions/zip_code"
      phone_number:
        "$ref": "/schemata/address#/definitions/phone_number"
    required:
      - supplier_id
      - address_type
      - street_address
      - city
      - state
      - country
    type:
    - object
  targetSchema:
    anyOf:
    - "$ref": "/schemata/address"
    - "$ref": "/schemata/error"
  title: Create
properties:
  id:
    "$ref": "/schemata/address#/definitions/id"
  supplier_id:
    "$ref": "/schemata/address#/definitions/supplier_id"
  address_type:
    "$ref": "/schemata/address#/definitions/address_type"
  street_address:
    "$ref": "/schemata/address#/definitions/street_address"
  city:
    "$ref": "/schemata/address#/definitions/city"
  state:
    "$ref": "/schemata/address#/definitions/state"
  country:
    "$ref": "/schemata/address#/definitions/country"
  zip_code:
    "$ref": "/schemata/address#/definitions/zip_code"
  phone_number:
    "$ref": "/schemata/address#/definitions/phone_number"
additionalProperties: false
id: schemata/address
