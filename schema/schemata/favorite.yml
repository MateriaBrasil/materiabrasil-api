---
"$schema": http://json-schema.org/draft-04/hyper-schema
title: MateriaBrasil - Favorite
description: A favorite is a material, product or service bookmarked by a user
stability: prototype
type:
- object
definitions:
  id:
    description: unique identifier of the favorite
    readOnly: true
    type:
    - integer
  album_id:
    description: the unique identifier of the album the favorite belongs to
    type:
    - integer
  favoritable_id:
    description: the id of the material, product of service the favorite is associated with
    type:
    - integer
  favoritable_type:
    description: the type of object the favorite is associated with
    type:
    - string
    enum:
    - Material
  favoritable:
    description: the object the favorite is associated with
    type:
    - object
    targetSchema:
      anyOf:
      - "$ref": "/schemata/material"
  identity:
    anyOf:
    - "$ref": "/schemata/favorite#/definitions/id"
links:
- description: Create a new favorite.
  href: "/favorites"
  method: POST
  rel: create
  schema:
    strictProperties: true
    properties:
      album_id:
        "$ref": "/schemata/album#/definitions/id"
      favoritable_id:
        "$ref": "/schemata/favorite#/definitions/favoritable_id"
      favoritable_type:
        "$ref": "/schemata/favorite#/definitions/favoritable_type"
    type:
    - object
  targetSchema:
    anyOf:
    - "$ref": "/schemata/favorite"
    - "$ref": "/schemata/error"
  title: Create
properties:
  id:
    "$ref": "/schemata/favorite#/definitions/id"
  album_id:
    "$ref": "/schemata/favorite#/definitions/album_id"
  favoritable_id:
    "$ref": "/schemata/favorite#/definitions/favoritable_id"
  favoritable_type:
    "$ref": "/schemata/favorite#/definitions/favoritable_type"
  favoritable:
    "$ref": "/schemata/favorite#/definitions/favoritable"
additionalProperties: false
id: schemata/favorite
